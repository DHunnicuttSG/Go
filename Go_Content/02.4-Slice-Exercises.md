Here are **Go slice challenges** organized from **easy → advanced**, specifically designed to strengthen your understanding of:

✅ slicing
✅ capacity vs length
✅ append behavior
✅ deletion patterns
✅ memory sharing
✅ real-world data manipulation (contacts, logs, etc.)

Each challenge includes a **goal, starter code**, and **hint**.

---

## 🟢 Level 1 – Fundamentals

### 1. Sum of a slice

**Goal:** Write a function that returns the sum of all ints in a slice.

```go
func sum(nums []int) int {
    // TODO
}
```

Example:

```go
sum([]int{1, 2, 3, 4}) → 10
```

**Hint:** Use a `for` loop + accumulator

---

### 2. Reverse a slice

Reverse a slice **in-place**.

```go
func reverse(nums []int) {
    // TODO
}

nums := []int{1,2,3,4}
reverse(nums)
fmt.Println(nums) // [4 3 2 1]
```

**Hint:** Swap front and back moving inward

---

### 3. Find max and min

Return both the min and max:

```go
func minMax(nums []int) (int, int) {
    // TODO
}
```

---

## 🟡 Level 2 – Intermediate

### 4. Remove duplicates

```go
func removeDuplicates(nums []int) []int {
    // TODO
}
```

Example:

```go
removeDuplicates([]int{1,1,2,2,3}) → [1,2,3]
```

**Hint:** Use a map or nested loop

---

### 5. Filter even numbers

```go
func onlyEven(nums []int) []int {
    // TODO
}
```

Example:

```
[1,2,3,4,5,6] → [2,4,6]
```

---

### 6. Insert an element at a position

```go
func insert(nums []int, index int, value int) []int {
    // TODO
}
```

Example:

```go
insert([]int{1,2,4,5}, 2, 3) → [1,2,3,4,5]
```

**Hint:** Use `append(slice[:i], append([]int{v}, slice[i:]...)...)`

---

## 🔵 Level 3 – Advanced

### 7. Safe delete (without modifying original)

```go
func deleteAt(nums []int, index int) []int {
    // return a NEW slice
}
```

Test:

```go
a := []int{10,20,30}
b := deleteAt(a, 1)
fmt.Println(a) // [10, 20, 30]
fmt.Println(b) // [10, 30]
```

---

### 8. Chunk a slice

Split into sub-slices of fixed size:

```go
func chunk(nums []int, size int) [][]int
```

Example:

```
[1,2,3,4,5,6,7], size=3
→ [[1,2,3],[4,5,6],[7]]
```

---

### 9. Circular rotate right

```go
func rotate(nums []int, k int) []int
```

Example:

```
[1,2,3,4,5], k = 2
→ [4,5,1,2,3]
```

---

## 🔴 Level 4 – Real-World

### 10. Contact Manager Slice Filter

Given:

```go
type Contact struct {
    ID int
    Name string
    Email string
}
```

Write:

```go
func findByEmail(contacts []Contact, email string) *Contact
```

Should return:

* pointer to contact if found
* `nil` if not

---

### 11. Merge two sorted slices

Example:

```
[1,3,5] + [2,4,6] → [1,2,3,4,5,6]
```

```go
func mergeSorted(a, b []int) []int
```

---

### 12. Concurrency challenge (advanced)

Process chunks in parallel (start simple):

```go
func parallelSum(nums []int, workers int) int
```

Hint: combine **slices + goroutines + channels**

---

## 🧪 Bonus (Trick Questions)

1. What happens here?

```go
a := []int{1,2,3}
b := a[:2]
b = append(b, 99)
fmt.Println(a)
```

2. Does this change the original?

```go
func modify(s []int) {
    s = append(s, 100)
}

nums := []int{1,2,3}
modify(nums)
fmt.Println(nums)
```

---
